{
  "name": "cdi-index",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build_site.mjs",
    "test": "node --test",
    "serve": "npx --yes serve ."
  }
}
